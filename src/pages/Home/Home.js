import React from 'react';
import DefaultNavbar from '../../components/DefaultNavbar';
import Header from './Header';

const Home = () => {
    return (
        <div>
            <DefaultNavbar />
            <Header />
        </div>
    );
};

export default Home;