import React from 'react';
import DefaultNavbar from '../../components/DefaultNavbar';

const Home = () => {
    return (
        <div>
            <DefaultNavbar />
            <h2>Home</h2>
        </div>
    );
};

export default Home;